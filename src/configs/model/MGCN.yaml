embedding_size: 64
n_ui_layers: 2
n_layers: 1

learning_rate_scheduler: [0.96, 50]
lambda_coeff: 0.9
reg_weight: 1e-04

knn_k: 10

learning_rate: 0.001  

cl_loss: [0.001,0.01,0.1] 

# D-ISOLATOR param
gamma: [0.0001, 0.001, 0.01, 0.1, 1, 2] 

# U-ISOLATOR param
alpha: [2, 6, 8, 10]
beta: [1, 5, 10, 15, 20]
gamma_infer: [0.0001, 0.001, 0.01, 0.1, 1, 2] 


hyper_parameters: ["cl_loss", "gamma"]
# hyper_parameters: ["alpha", "beta", "gamma_infer", "cl_loss", "gamma"]

